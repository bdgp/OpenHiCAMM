#!/usr/bin/env bash
set -eu -o pipefail
if [[ $# -lt 2 ]]; then
  echo "Usage: $0 mm-dir fiji-dir" >&2
  exit 1
fi
cd "$1"
cp -vn MMConfig_demo.cfg MMCorePy.py MMCoreWrapDemo.py _MMCorePy.so libMMCoreJ_wrap.jnilib libgphoto2 libmmgr_dal_* luts macros mmautofocus mmplugins plugins scripts "$2"
